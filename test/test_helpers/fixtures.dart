// Shared sample domain objects (a reusable test Food, DiaryEntry, etc.) so
// every test file builds on the same fixtures instead of hand-building them.
//
// Track 1 has no domain objects yet — this file is created empty so later
// tracks (2+) fill it in as they add entities.

// coverage:ignore-file
library;