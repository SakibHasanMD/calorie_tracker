name: calorie_tracker
description: A simple calorie tracking app (Version 0 / MVP).
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  # Local SQLite database
  sqflite: ^2.3.0
  path: ^1.8.3

  # Date formatting (today/yesterday/July 24 etc.)
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
